/* eslint-disable no-unused-vars */
/*module.exports = {
	name: 'ping',
	description: 'Ping!',
	execute(message, args, client) {
		var botping = Math.round(bot.ws.ping)

		message.channel.send(`My Ping is ${botping}!`)
	}
};*/
